extension Constelation {
    struct Constants {
        struct distance {
            static let min = Double(1600)
        }
        
        static let radius = Range(uncheckedBounds: (3.0, 20.0))
    }
}
